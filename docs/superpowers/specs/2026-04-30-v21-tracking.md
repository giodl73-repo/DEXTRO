# v2.1 Tracking: Deferred Items from 9-Role Re-Review
**Date:** 2026-04-30
**Owner:** Gio Della-Libera
**Status:** Open — track during implementation

## Purpose

The 2026-04-30 v2 9-role re-review **lifted the SCALE BLOCK** but surfaced ~30 REVISE items. The highest-impact blockers were patched into the v2 specs themselves (commit `<v21>`); this doc tracks the rest so they're visible during implementation without blocking plans.

Each item is scoped to either: (a) the relevant per-capability implementation plan, (b) a one-shot follow-up doc (e.g., `docs/file-formats/manifests.md`), or (c) deferred until a real failure forces it.

## Status legend

- **(plan)** — address in the per-capability implementation plan
- **(followup-doc)** — write a one-shot doc; reference from specs
- **(defer)** — don't fix until a real user / test fails

---

## MERIDIAN — cross-spec consistency

| ID | Item | Disposition | Notes |
|---|---|---|---|
| M-01 | `--label` overload — rename `--label` → `--plan-label` / `--comments-label` / `--ensemble-label` everywhere | (plan) | Patched in plan-comparison + deposition. Apply same rename in researcher-toolkit (`validate-against-ensemble`) and state-staff-interop import/export examples during their plans. |
| M-02 | Rename `validate-against-ensemble` → `redist research validate-ensemble` | (plan) | Researcher Toolkit plan. |
| M-03 | `docs/file-formats/manifests.md` — canonical manifest field inventory referenced by all specs | (followup-doc) | Write before Court Reports implementation; all manifest-emitting commands reference it. |
| M-04 | Path-portability rule (relative-to-package-root) explicitly applied to `narrative_manifest.json` and `what_if/.../manifest.json` | (plan) | Already implied in v2.1 narrative manifest patch; make explicit during Plan Comparison + Deposition implementation. |
| M-05 | Roadmap persona-table cell for §2 expert: add "fast iterative re-analysis at trial" to "What's missing" | (plan) | Roadmap edit during Deposition Prep implementation. |

---

## DATUM — data integrity, citation, replication

| ID | Item | Disposition | Notes |
|---|---|---|---|
| D-01 | `accessed_date` vs `fetched_at` semantics — pick one, document the rule for citations vs. manifests | (followup-doc) | Goes in `docs/file-formats/manifests.md`. |
| D-02 | Onboarding §3a: name the committed file path `examples/vermont-walkthrough/checksums.json` | (plan) | Already added to DoD in v2.1 patch; concretize the schema in Onboarding plan. |
| D-03 | `sources.json` registry needs `schema_version` + content-hash field; snapshot at report-generation time | (plan) | Court Reports plan; existing infra to extend. |
| D-04 | Deterministic Typst PDF + zip SHA — `SOURCE_DATE_EPOCH`, `tar --sort=name --mtime=...`, zeroed PDF metadata | (plan) | Court Reports plan; specify exact env. |
| D-05 | AEA REPRODUCE.sh — declare *target* platform (Linux x86_64 glibc 2.35); add `Cargo.lock` SHA + `rust-toolchain.toml` SHA; add `requirements.lock` (uv/pip-tools) | (plan) | Researcher Toolkit plan. |
| D-06 | `docs/file-formats/citation-strings.md` — defines depo + civic citation strings; declares Chicago default scope | (followup-doc) | Write before Court Reports implementation. |

---

## SCALE — statistical rigor (BLOCK lifted; REVISE items remain)

| ID | Item | Disposition | Notes |
|---|---|---|---|
| S-01 | `docs/parameters/whitelist-dependencies.md` — DAG of which whitelist tweaks invalidate which downstream computations (e.g., `bloc_cluster_unit` invalidates Holm correction, `bloc_p_value_method=none` blocks headline-significance language) | (followup-doc) | Required for Deposition Prep implementation. |
| S-02 | Holm family in Callais must include leave-one-out sensitivity-variant runs from Civic Bidirectional conflict resolution | (plan) | Callais Evidence Layer plan. |
| S-03 | Researcher ensemble diagnostics: require ≥4 parallel chains for Gelman-Rubin R-hat <1.05; document ESS as on summary statistics not partitions; add partition-distance autocorrelation (Hamming) | (plan) | Researcher Toolkit plan. |
| S-04 | Plan Comparison MoE suppression criterion (line 33) — formally define "direction change" for non-monotone metrics like MM count | (plan) | Plan Comparison plan. |

---

## BENCHMARK — test coverage

| ID | Item | Disposition | Notes |
|---|---|---|---|
| B-01 | Court Reports value-correctness tests — PDF text extraction asserting hand-computed numbers; deliberately-malformed Typst output asserts `verapdf` gate fails | (plan) | Court Reports plan. **P0**. |
| B-02 | Callais Evidence Layer named L0 anchors — OLS coefficient ±0.02 on synthetic ground truth; Holm dominates raw on 30-test family; VIF>5 sets underpowered flag; `independently_verified=false` injects caveat | (plan) | Callais plan. **P0**. |
| B-03 | Daemon p99 benchmark — runner class pinned in roadmap CI strategy (v2.1 patch); spec implementation needs warm-up of N=5 discarded evals; separate VT (PR) vs LA (nightly) budgets | (plan) | Deposition Prep plan. |
| B-04 | Civic Bidirectional L2 "if available" replaced with checked-in fixture `tests/fixtures/civic/la_2024_round/` (sanitized) | (plan) | Civic Bidirectional plan. |
| B-05 | Hermetic Districtr fixture under `tests/fixtures/districtr/` instead of CDN-fetch | (plan) | State Staff Interop plan. |
| B-06 | Researcher kernel-state attestation — pin to compatible *range*, not single version | (plan) | Researcher Toolkit plan. |
| B-07 | Specify build-commit spoof mechanism — `REDIST_BUILD_COMMIT` env override | (plan) | Deposition Prep plan. |
| B-08 | Civic race-conflict sensitivity test asserts the *result* (e.g., disagreement above threshold X triggers `robust=false` flag), not just invocation | (plan) | Civic Bidirectional plan. |
| B-09 | Synthetic ensemble diagnostics reframed as implementation-correctness; add separate nightly N≥10k statistical-anchor test | (plan) | Researcher Toolkit plan. |
| B-10 | Court Reports L2 stronger assertions — section-header presence + ordering, not just "page count is reasonable" | (plan) | Court Reports plan. |

---

## TRENCH — implementation pitfalls (PP-XX numbered)

PP-18 through PP-22 resolved in v2 (with the v2.1 PP-20 bash fix). New items from v2 review:

| PP | Item | Disposition | Notes |
|---|---|---|---|
| PP-23 | Daemon orphaned socket / stale lock — startup connect-then-unlink protocol | Patched in v2.1 deposition spec | Implementation in Deposition Prep plan. |
| PP-24 | Daemon SIGTERM mid-eval — two-phase shutdown drain | Patched in v2.1 deposition spec | Implementation in Deposition Prep plan. |
| PP-25 | Whitelist dependency map — file format unspecified | (followup-doc) | See SCALE S-01. |
| PP-26 | Windows named-pipe vs Unix socket — split flags | Patched in v2.1 deposition spec | Implementation in Deposition Prep plan. |
| PP-27 | Civic CSV BOM — UTF-8 BOM-tolerant reader, declared encoding policy | Patched in v2.1 civic spec | Implementation in Civic Bidirectional plan. |
| PP-28 | Civic GEOID leading-zero loss detection | Patched in v2.1 civic spec | Implementation in Civic Bidirectional plan. |
| PP-29 | Civic localhost rejection by parsed-IP not string-match | Patched in v2.1 civic spec | Implementation in Civic Bidirectional plan. |
| PP-30 | URL snapshot bounded fetch / no-creds / dynamic-page handling | Patched in v2.1 civic spec | Implementation in Civic Bidirectional plan. |
| PP-31 | Narrative manifest path with spaces / non-ASCII — store relative paths | Patched in v2.1 plan-comparison spec | Implementation in Plan Comparison plan. |
| PP-32 | Typst stderr swallowed by wrapper — surface in manifest, fail on warnings | (plan) | Court Reports plan. |
| PP-33 | Schema-version handshake fingerprint collision — multi-attribute fingerprint | (plan) | State Staff Interop plan. |
| PP-34 | Unicode/CP1252 console policy — add "All CLI stdout/stderr is ASCII-only on Windows; Unicode is permitted in file outputs only" to `docs/error-conventions.md` | (followup-doc) | One-line addition; do during Onboarding plan. |

---

## SURVEY — outside-in user experience

| ID | Item | Disposition | Notes |
|---|---|---|---|
| SUR-01 | Onboarding lines 132–134 + 158: Louisiana residual fragments | Patched in v2.1 onboarding spec | None. |
| SUR-02 | Civic Bidirectional COI example — add a VT/New England example alongside the Lafayette one | (plan) | Civic Bidirectional plan; cosmetic. |

---

## COVENANT — provenance & chain of custody

| ID | Item | Disposition | Notes |
|---|---|---|---|
| C-01 | Deposition log canonical JSONL + hash chain + sidecar manifest + discoverability note | Patched in v2.1 deposition spec | Implementation in Deposition Prep plan. |
| C-02 | Civic URL snapshot protocol (WARC fallback, bounds, no-creds) | Patched in v2.1 civic spec | Implementation in Civic Bidirectional plan. |
| C-03 | `narrative_manifest.json` records `plan_a_manifest_sha256` / `plan_b_manifest_sha256` / `baseline_manifest_sha256` | Patched in v2.1 plan-comparison spec | Implementation in Plan Comparison plan. |
| C-04 | Court Reports rebuild block links to bootstrap with time estimates + common failures | Patched in v2.1 court-reports spec | Implementation in Court Reports plan. |
| C-05 | Import manifest captures `import_compat_sha256` of the active version-pin lookup | (plan) | State Staff Interop plan. |

---

## COMMONS — civic-facing usability

| ID | Item | Disposition | Notes |
|---|---|---|---|
| CM-01 | `quickstart-civic-advocate.md` documents obtaining the state's plan when not Districtr-published (shapefile/GeoJSON path) | (plan) | Onboarding plan. |
| CM-02 | Court Submission Report methodology section credits civic data contributors above the fold | (plan) | Court Reports plan. |
| CM-03 | Sheets template + plain-English how-to land *with* the Civic Bidirectional spec, not after | (plan) | Civic Bidirectional plan. |
| CM-04 | Dogfood test names a *small* civic group (neighborhood association), not a state chapter | (plan) | Civic Bidirectional plan; DoD edit. |

---

## BOUNDARY — legal & evidentiary

| ID | Item | Disposition | Notes |
|---|---|---|---|
| BD-R1 | Civic inputs ingested with `--validate {lenient,advisory}` MUST NOT appear in court-mode reports without explicit `--allow-non-strict-civic` and a manifest annotation | (plan) | Court Reports plan. |
| BD-R2 | Race-of-candidate `attestation-doc` — accepted formats, reproducibility-zip inclusion, `attestation_doc_sha256` + `attestation_doc_format` in `race_of_candidate_provenance` JSON | (plan) | Callais Evidence Layer plan + Civic Bidirectional plan (matched edits). |
| BD-N1 | Deposition log discoverability — `--no-log` flag for work-product privilege | Patched in v2.1 deposition spec | None. |
| BD-N2 | `--enforce-build-commit` should default-on in `deposition-server --case-mode` | (plan) | Deposition Prep plan. |
| BD-N3 | Civic-counter-proposal watermark on the comparison summary card PNG (not just narrative) | Patched in v2.1 plan-comparison spec | Implementation in Plan Comparison plan. |

---

## Summary

- **Patched in v2.1 specs (this commit)**: 13 items
- **Deferred to per-capability plans**: 23 items
- **Deferred to followup-docs**: 4 items (`manifests.md`, `citation-strings.md`, `whitelist-dependencies.md`, `error-conventions.md` Unicode/CP1252 line)
- **Total tracked**: 40

Implementation order (per roadmap): Onboarding → Callais → Court Reports → State Staff Interop → Plan Comparison → Civic Bidirectional → Deposition Prep → Researcher Toolkit. Each plan opens with a "v2.1 items addressed by this plan" checklist drawn from this doc.
